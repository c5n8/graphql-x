type Query {
  query: Boolean
}

extend type Query {
  query2: Boolean
}

type Mutation {
  mutation: Boolean
}

extend type Mutation {
  mutation2: Boolean
}

type Subscription {
  subscription: Boolean
}

extend type Subscription {
  subscription2: Boolean
}
