scalar DateTime

directive @create on OBJECT

directive @readonly on FIELD_DEFINITION

type Post @create {
  id: ID!
  createdAt: DateTime @readonly
  title: String!
  body: String
  category: Category
}

type Category {
  id: ID!
}
